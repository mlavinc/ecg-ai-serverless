# Override the API base URL for local development.
# In production this is intentionally left unset: CloudFront serves both the
# frontend and /api/* from the same domain, so the default relative "/api"
# base URL (see src/services/api/client.ts) requires no configuration.
VITE_API_BASE_URL=
